# lair app

TanStack Start app package inside the Nx workspace.

Run from the workspace root:

```bash
npx nx dev lair
npx nx build lair
npx nx test lair
npx nx run lair:typecheck
npx nx storybook lair
```

The app keeps the initial Tailwind and Storybook setup from the starter.
