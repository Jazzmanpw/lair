# lair workspace

Nx workspace for the TTRPG app prototype.

Primary app:

- `apps/lair`

Common commands are run directly through Nx, for example:

```bash
npx nx dev lair
npx nx build lair
npx nx test lair
npx nx run lair:typecheck
npx nx storybook lair
```
