# UI Archive

This library stores archived UI prototypes. Archived prototypes remain available as design evidence, but they are not the default foundation for current Running Mode work.

All older implementation iterations are closed historical UI prototypes. Store them under `src/<iteration-number>/`.

## Current iteration archive

The current implementation iteration is 4. Archived work from the current iteration also lives under `src/4/`. Use a descriptive subfolder name that explains what was archived.

- `src/4/running-mode-layout-r1` - Layout R1 was discarded as the active layout foundation after exposing weak context in the initial prompt. Its useful inputs were carried forward into Running Mode R2 framing.

When the current implementation iteration wraps up, update the current iteration number here and revise this section so previously current archive entries become ordinary historical iteration entries.
